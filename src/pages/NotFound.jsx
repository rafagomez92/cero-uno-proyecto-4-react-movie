import React from 'react';
import PageNotFound from '../components/PageNotFound';

const NotFound = () => {
    return(
        <div>
            <PageNotFound />
        </div>
    );
}

export default NotFound;