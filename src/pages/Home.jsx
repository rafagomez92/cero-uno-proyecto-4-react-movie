import React from 'react';
import MovieContainer from '../containers/MovieContainer';

const Home = () => {
    return(
        <div>
            <MovieContainer />            
        </div>
    );
}

export default Home;